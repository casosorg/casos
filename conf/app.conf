appname       = casos
httpport      = 9000
runmode       = dev
SessionOn     = true
copyrequestbody = true

; -- Database ---------------------------------------------------------------
driverName    = sqlite
dataSourceName=
dbName        = casos
kineEndpoint  =

; -- Authentication ---------------------------------------------------------
; local   = initialize the built-in admin account on first visit (default)
; casdoor = use Casdoor OAuth/OIDC; all Casdoor settings below are required
authMode = local
; One-time token for first-run setup from another machine (e.g. behind a
; reverse proxy). When blank, CasOS generates one at startup and prints it to
; the server log. Setup from the machine running CasOS never needs a token.
localSetupToken =
sessionLifetime = 86400

casdoorEndpoint     =
clientId            =
clientSecret        =
casdoorOrganization =
casdoorApplication  =

; -- Outbound proxy (optional) ----
; Leave blank to use environment proxy settings or direct access.
; When set, the proxy is required for requests not matched by NO_PROXY.
socks5Proxy   =

; -- Built-in image registry mirror -----------------------------------------
; auto   = probe each canonical registry from the target worker and enable
;          only the mirrors needed by that worker (default)
; always = always route built-in image pulls through the mirrors
; never  = always pull from the canonical registries
imageRegistryMirror = auto

; -- Helm images (optional) -------------------------------------------------
; Set true only when implicit/latest chart images must use IfNotPresent.
helmImplicitLatestPullPolicy = false
; Maximum time Helm waits for an application and its jobs to become ready.
helmInstallTimeout = 20m

; -- Control plane ----------------------------------------------------------
apiserverPort = 6443
apiserverBind = 127.0.0.1
dataDir       = ./data
ingressControllerEnabled = true
serviceLBEnabled = true
